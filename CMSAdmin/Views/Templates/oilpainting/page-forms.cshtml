@inject PagePayload CmsPage

@{
	Layout = "_main.cshtml";
}

@section SubContent {
	<div>
		@await Html.PartialAsync("_loginout")
	</div>
	<div>
		@await Html.PartialAsync("_forgot")
	</div>
	<div>
		@await Html.PartialAsync("_reset")
	</div>
	<div>
		@await Html.PartialAsync("_changepass")
	</div>
	<div>
		@await Html.PartialAsync("_changeprofile")
	</div>
}

@section SideBar {
	<ul>
		<li>
			@if (CmsPage.ChildNav.Any()) {
				<h2>Child Pages</h2>
				<ul class="sidemenu">
					@foreach (var itm in CmsPage.ChildNav) {
						<li><a href="@itm.FileName">@itm.NavigationText</a></li>
					}
				</ul>
			}
		</li>
		<li>
			@if (CmsPage.SiblingNav.Any()) {
				<h2>Sibling Pages</h2>
				<ul class="sidemenu">
					@foreach (var itm in CmsPage.SiblingNav) {
						<li><a href="@itm.FileName">@itm.NavigationText</a></li>
					}
				</ul>
			}
		</li>
	</ul>
}